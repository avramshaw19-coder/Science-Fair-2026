Sanity check (important)

For a given N, all five should print the same primes= and checksum=.

Example approach:

Pick N = 100000 first (quick check).

Confirm primes/checksum match across languages.

Then scale N up until each run is ~30–120 seconds.